# infra/app/docker-compose.app.yml ile kullanım:
#   Bu dosyayı `infra/app/.env.app` olarak kopyalayın ve düzenleyin.
#   Ardından (önerilir) compose'un build sırasında değişkenleri görmesi için:
#     cd infra/app && ln -sf .env.app .env
#   Docker Compose, compose dosyasıyla aynı klasördeki `.env` dosyasını otomatik okur;
#   `env_file: .env.app` yalnızca container çalışırken geçerlidir — web imajı build-arg için `.env` veya
#   `docker compose ... --env-file infra/app/.env.app` kullanın.
# Gerçek sırları repoya commit etmeyin.
#
# UYARI — Docker içinde API: DATABASE_URL içinde ASLA "localhost" veya "127.0.0.1" kullanmayın.
# Bunlar container'ın kendisini işaret eder; Postgres host makinedeyse host adı şu olmalı:
#   host.docker.internal  (Docker Desktop Mac/Win; compose'ta extra_hosts tanımlı)
# apps/api/.env'ten kopyaladıysanız localhost satırını mutlaka buna çevirin.

# --- Web imajı build arg (compose --env-file ile okunur) ---
# Tarayıcının göreceği API adresi (host makineden erişim: localhost yeterli)
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# İsteğe bağlı port publish
# API_PORT=3001
# WEB_PORT=3000

# --- API container ortamı (.env.app içinde api ile paylaşılır) ---
# Host’taki PostgreSQL (Docker Desktop Mac/Win). Parola repoda yok — .env.app içinde gerçek değeri verin.
# Fortify uyumu: URI içinde gerçekçi şifre örneği kullanılmaz.
DATABASE_URL=postgresql://dbuser:__SET_IN_ENV__@host.docker.internal:5432/dbname

# JWT ve diğer: apps/api/.env.example ile aynı anahtarlar
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Container içinde production: CORS_ORIGIN zorunlu (virgülle ayrılmış tam origin)
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000

# İsteğe bağlı — yapılandırılmış loglarda git meta (API Dockerfile build-arg ile de set edilir)
# GIT_COMMIT=
# GIT_BRANCH=

NODE_ENV=production

# İsteğe bağlı — container içinde dosya logu yerine boş bırakılabilir
# API_JSON_LOG_FILE=

# AI / Twilio / Redis vb. gerekiyorsa apps/api/.env ile aynı anahtarları ekleyin
