# Next.js — kopyala: cp apps/web/.env.example apps/web/.env.local
#
# next dev: tarayıcı /api/v1 kullanır; rewrite hedefi INTERNAL_API_URL (yoksa localhost:3002).
# API’nin gerçek PORT’u ile aynı olmalı (apps/api/.env içindeki PORT).
# INTERNAL_API_URL=http://127.0.0.1:3002
#
# next start / Docker web: rewrite yok; tarayıcı doğrudan NEXT_PUBLIC_API_URL kullanır (build zamanı).
NEXT_PUBLIC_API_URL=http://localhost:3002/api/v1
